services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bellaagenda-frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:3000/api
    env_file:
      - path: .env
        required: false
